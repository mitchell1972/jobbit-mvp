import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";
import { Header } from "@/components/Header";
import { Footer } from "@/components/Footer";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Jobbit — AI-Powered Career Navigation for Skilled Trades",
  description:
    "Discover your ideal skilled trade career. Take our AI-powered quiz to get matched with trade careers, apprenticeships, and certification programs.",
  keywords: [
    "career quiz",
    "skilled trades",
    "apprenticeship",
    "trade careers",
    "AI career matching",
    "union jobs",
    "certification programs",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} antialiased`}>
        <a href="#main-content" className="skip-link">
          Skip to main content
        </a>
        <Header />
        <main id="main-content">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
